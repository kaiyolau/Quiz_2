<!DOCTYPE html>
<html>
  <head>
    <title>Idea Factory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%#= javascript_importmap_tags %>
  </head>

  <body>


        <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <div class="me-5">
            <a class="navbar-brand" href="/">
                <img src="https://d3ui957tjb5bqd.cloudfront.net/uploads/2015/09/airbnb-2.jpg" alt="idea pic" width="50" height="50">
            </a>
        </div>

        <a class="navbar-brand" href="<%= root_path %>">Idea Factory</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link"  href="<%= ideas_path %>">Ideas</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%= new_idea_path %>">Post Idea</a>
            </li>

            <% if user_signed_in? %>
              <li class="nav-item">
                <a class="nav-link disabled">Hello <%= current_user.name %> </a>
              </li>
              <li class="nav-item">
              <div><%= button_to "Log out", session_path, method: :delete, class: "btn btn-danger" %></div>
              </li>

            <% else %>
              <li class="nav-item">
                <div class="nav-link">
                  <a class="btn btn-primary btn-sm" href="<%= new_session_path %>">Sign In</a>
                </div>
              </li>
              <li class="nav-item">
                <div class="nav-link">
                  <a class="btn btn-primary btn-sm" href="<%= new_user_path %>">Sign Up</a>
                </div>
              </li>
            <% end %>
          </ul>

        </div>
      </div>
    </nav>

    <% flash.each do |type, message| %>
      <div class="alert alert-info" role="alert">
        <%= type + "------" + message %>
      </div>
    <% end %>






    <%= yield %>
  </body>
</html>
